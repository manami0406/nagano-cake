<h1>商品一覧</h1>

<% @items.each do |item| %>

<% if item.image.attached? %>
  <%= image_tag item.image, size: "200x200" %>
<% else %>
  <%= image_tag 'no-image', size: "200x200" %>
<% end %>
<%= item.name %>
<%= item.price %>

<% end %>